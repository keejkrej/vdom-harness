# Tweet pack

## 1 — main

Pi customizes an agent.
DeepSeek Harness composes a runtime.
vdom reconciles a society.

You do not spawnAgent(). You declare the computational society you want. A reconciler diffs topology — mount, update, retain, unmount — the way React diffs a tree.

## 2 — reply

The clip is a replay of a real TypeScript run, not a mock.

A paper compiles to an AgentGraph.
One-shot scores 0.00 on "dom virtual".
A scientist emits a replacement graph.
Reconcile: = solve  + critic  + refine
Score: 1.00. Output: mod lautriv.

Not a chat UI. A reconciler.

## 3 — optional

Papers can synthesize topology, prompts, memory, routing. They cannot yet emit arbitrary tools.

Next primitive: a Capability node — generate source, sandbox it, test it, mount it.

## og

Dark product teaser of vdom, a virtual DOM for agents. Left: a live AgentGraph, solve retained, critic and refine mounting in mint. Right: a trace log of the word-reverse puzzle and a score flipping from 0.00 to 1.00. Bottom: three-column compare — Pi customizes an agent, DeepSeek Harness composes a runtime, vdom reconciles a society.
